# Tests para la aplicación principal
import pytest

def test_root_endpoint(client):
    # Test del endpoint raíz
    # response = client.get("/")
    # assert response.status_code == 200
    # assert response.json() == {"message": "API levantada"}
    pass